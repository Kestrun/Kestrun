permissions:
    contents: read
    pull-requests: read

name: CI (PR)

on:
    pull_request:
        paths:
            - "**/*.cs"
            - "**/*.csproj"
            - "**/*.sln"
            - "**/*.ps1"
            - "**/*.psm1"
            - "**/*.psd1"
            - "**/*.yml"
            - "**/*.yaml"
            - tests/**

jobs:
    ci:
        uses: Kestrun/Kestrun/.github/workflows/ci-core.yml@main
        with:
            is_pr: true
