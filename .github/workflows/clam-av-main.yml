permissions:
    contents: read
    pull-requests: read

name: AV Scan (main)

on:
    push:
        branches: [main]
        paths:
            - "**/*.cs"
            - "**/*.csproj"
            - "**/*.sln"
            - "**/*.ps1"
            - "**/*.psm1"
            - "**/*.psd1"
            - "**/*.yml"
            - "**/*.yaml"
            - tests/**
    workflow_dispatch: {}

jobs:
    clamav:
        uses: Kestrun/Kestrun/.github/workflows/clam-av-core.yml@main
        with:
            is_pr: false
