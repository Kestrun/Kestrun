permissions:
    contents: read
    pull-requests: read

name: CI (main)

on:
    push:
        branches: [main]
        paths:
            - "**/*.cs"
            - "**/*.csproj"
            - "**/*.sln"
            - "**/*.ps1"
            - "**/*.psm1"
            - "**/*.psd1"
            - "**/*.yml"
            - "**/*.yaml"
            - tests/**
    workflow_dispatch: {}

jobs:
    ci:
        uses: ./.github/workflows/ci-core.yml
        with:
            is_pr: false
